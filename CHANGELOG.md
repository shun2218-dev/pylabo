# 変更履歴

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [未リリース]

### 追加

- **全 52 レッスンに「もっと詳しく」を用意した**（従来は 15 レッスンのみ）。解説は演習に要る分だけに絞ってあるため、「サンプルには出てこないが、もっと良い書き方がある」状態が起きていた。各レッスンの末尾に、別のやり方・より適した道具・つまずきやすい前提を 1〜2 段落で書き、公式ドキュメントへの入り口を置いた
- `npm run verify:links` — 本文から張っている外部リンク（103 本）が到達できるか、`#anchor` が生きているかまで確かめる。リンク切れは学習者を行き止まりに連れていくため
- **段階ヒント** — 演習の `hint` を配列で書けるようにした。ヒントが 1 段ずつ開き、「どこまで自力で進めて、どこで詰まっているか」に合わせて必要なぶんだけ見られる。複雑な演習 18 件に導入（基本文法 3 / データ分析 3 / Web・自動化 3 / FastAPI 4 / テストと品質 5）

### 修正

- 「HTTP と API の基礎」の requests クイックスタートへのリンクが 404 になっていたので、現行の URL に差し替えた

### 変更

- ヒントの方針を「解答例に導く手順」から「**ロジックの手がかり**」に改めた。`solution` はサイトが用意した書き方のひとつでしかないので、ヒントをたどると解答例の書き方に収束する、という作りをやめた。解答例のブロックにも「違う書き方でも正解」であることを明記した

## [1.1.0] - 2026-08-01

### 追加

- **テストと品質**（実践・12 レッスン）を公開 — `assert` とミニテストランナーの自作から始め、pytest・例外と `approx`・`parametrize`・フィクスチャ・モック・テストしやすい設計・カバレッジを経て、TDD と回帰テストまで
- ブラウザ内で**本物の pytest を実行**できるようにした。`run_pytest()` が、いまエディタに書かれているコードをそのままテストファイルとして書き出して pytest に渡す（失敗レポートの行番号はエディタの行と一致する）
- 同梱パッケージに `pytest` と `coverage` を追加（wheel 約 2.7MB。テストと品質コースに到達した学習者だけがダウンロードする）
- テストの演習は「**壊した実装に当てて落ちるか**」で採点する。テストが書けたことではなく、そのテストがバグを捕まえられるかを確かめる
- 演習に「**採点が落とせないといけない回答**」（`rejects`）を書けるようにした。`verify:exercises` が毎回それを実行し、通ってしまわないかを確かめる
- `verify:exercises` が**解説中のコード例も実行**するようになった（全 100 例）。エラーで止まらないこと、および出力が記録（`src/courses/__snapshots__/example-output.json`）と一致することを見る。パッケージ更新で学習者の画面の表示が変わったときに、本文の説明とのずれに気づける
- わざとエラーになるコード例に印（`raises`）を付け、「**エラーで止まること**」も確認対象にした
- `npm run verify:setup` — 学習者のブラウザ内（Pyodide）と同じ版の Python・パッケージを `.venv-pyodide/` に用意する。採点の期待値は実際の計算結果なので、手元の別の版で確かめても保証にならない
- `node scripts/audit-grading.mjs` — 解答例を少しだけ間違えた版（数値をずらす・比較を反転する・文を消す）に書き換え、それでも採点を通ってしまう箇所を洗い出す調査用スクリプト

### 修正

- **採点の甘さを 3 件修正**（テストと品質コース）。「`assert` で確かめる」「テストしやすい設計」「先にテストを書く」は、テストの**個数**しか見ていなかったため、`assert True` のような中身のないテストでも合格になっていた。学習者のテストを壊した実装に当てて、落ちることを確かめるようにした（課題文にもその旨を明記）
- コード例の検証で、検証の仕組み自身のファイルが `Path(".").iterdir()` の出力に混ざっていた問題を修正
- 集合（set）を `print` したときの並びが実行ごとに変わりうることを、「タプル・集合・while」の本文に明記した

### 変更

- コースカードの難易度バッジ（入門 / 中級 / 実践）を、難易度ごとに色分けした塗りのバッジにした。従来は枠線と淡い文字色だけで、カード背景に同化して読みにくかった
- 「追加予定」カードの収録内容の箇条書きを下端で揃えた。説明文の行数がカードごとに違うため、開始位置がずれて見えていた

[1.1.0]: https://github.com/shun2218-dev/pylabo/releases/tag/v1.1.0

## [1.0.0] - 2026-07-30

初回リリース。

### 学習コンテンツ

- **Python の基本文法**（入門・17 レッスン） — 変数から条件分岐・コレクション・関数・短く書く記法・内包表記・例外・クラスまで
- **データ分析・集計**（中級・10 レッスン） — 素の Python での集計から pandas、groupby、matplotlib による可視化、売上レポートの作成まで
- **Web/API・自動化**（中級・7 レッスン） — pathlib・CSV/JSON・正規表現・日時・HTTP を扱い、ログ集計ツールを仕上げる
- **アプリ開発（FastAPI）**（実践・6 レッスン） — ルーティングと入力検証を自作して仕組みを理解し、FastAPI で書き直す

追加予定として、テストと品質・型ヒントと静的解析・データベースと SQL・非同期処理・環境とパッケージング・CLI ツール開発の 6 コースを一覧に掲載しています。

### アプリ

- Pyodide を同梱し、インストールなしでブラウザ内の Python を実行
- 解説中のコードはその場で編集・実行できる
- 演習は Python 側の `check()` による自動採点。チェック項目ごとに合否を表示
- pandas / matplotlib のグラフをページ内に表示
- 進捗と書きかけコードを localStorage に保存
- Python は Web Worker で実行し、無限ループも「停止」で復帰できる
- ダーク / ライトテーマ

### 基盤

- 依存はすべて npm 管理。実行時に外部 CDN を参照しない
- ホーム画面の初期読み込みを分割（コース本文・エディタ・Markdown は開いたときに読み込む）
- Vitest によるユニットテスト、および全演習の解答例が採点を通るかの検証スクリプト
- GitHub Actions で型チェック・テスト・ビルド・演習検証を実行
- canonical / OGP / sitemap を出力。公開 URL は Vercel の環境変数から自動判定

[1.0.0]: https://github.com/shun2218-dev/pylabo/releases/tag/v1.0.0
